# Use Cases

- Run a local TCP cache and issue commands manually with `nc`.
- Read tests to understand command semantics.
- Extend the server with one new Redis-like command.
- Compare text protocol limitations with RESP later.
